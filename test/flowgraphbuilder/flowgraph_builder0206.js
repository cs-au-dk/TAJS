A = d(u, v, b, c);
if (A <= 5)c = true; else {
    g.x = Math.abs(y.x) / A;
    g.y = Math.abs(y.y) / A;
    s = 1 / fps * (35 / j);
    k.x = g.x * s;
    k.y = g.y * s;
    u += k.x * (b < u ? -1 : 1);
    v += k.y * (c < v ? -1 : 1);
    c = false
}