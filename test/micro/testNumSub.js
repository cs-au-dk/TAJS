var num = Math.floor(Math.random());

var constant = 1;

var num_num = num - num;

var num_constant = num - constant;

var constant_num = constant - num;

TAJS_dumpValue(num);
TAJS_dumpValue(constant);
TAJS_dumpValue(num_num);
TAJS_dumpValue(num_constant);
TAJS_dumpValue(constant_num);